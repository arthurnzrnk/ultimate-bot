# Example environment configuration for the Ultimate Bot backend
PORT=8000
# Comma‑separated list of allowed origins for CORS (if using a frontend on another port)
CORS_ORIGINS=http://localhost:5173
# Starting equity for paper trading
START_EQUITY=10000
# Default email sender address for notifications
EMAIL_FROM=no-reply@ultimate-bot.local
# SMTP configuration (leave empty to disable real email sending)
EMAIL_SMTP_HOST=
EMAIL_SMTP_USER=
EMAIL_SMTP_PASS=
