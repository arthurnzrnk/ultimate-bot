# Ultimate Bot — Strategy V3.4 backend

PORT=8000
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Paper account
START_EQUITY=10000

# -------- Data feed --------
# Race a minimal, reliable feed (Binance book + CBX ticker)
MINIMAL_FEED=1

# Synthetic top-3 depth notional (USD) until real depth wired
SYN_TOP3_NOTIONAL=75000

# -------- Venue fees & ticks (copy your real venue values) --------
# Per side
FEE_MAKER_BPS_PER_SIDE=4          # 0.04%
FEE_TAKER_BPS_PER_SIDE=10         # 0.10%
ASSUME_TAKER_EXIT_ON_STOPS=false  # true if your venue uses market-only stops

# Exchange constraints
EXCHANGE_MIN_NOTIONAL=0           # if >0, engine enforces +5%
PRICE_TICK=0                      # e.g., 0.10
QTY_TICK=0                        # e.g., 0.0001

# -------- Execution & slip guard --------
SPREAD_CAP_BPS_M1=8
SLIP_COEFF_K=0.6
TOP3X_ORDER_NOTIONAL_MIN=2.0
MAX_SHRINK_ITERS=12
MIN_NOTIONAL_USD=0
